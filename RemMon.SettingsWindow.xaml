<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="RemMon.SettingsWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" Title="Настройки" Width="1180" Height="860" MinWidth="1000" MinHeight="720" WindowStyle="None" ResizeMode="CanResize" WindowStartupLocation="Manual" Background="#FF07111B" UseLayoutRounding="True" SnapsToDevicePixels="True" xmlns:remmon="clr-namespace:RemMon;assembly=RemMon">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False" />
  </shell:WindowChrome.WindowChrome>
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <SolidColorBrush x:Key="SettingsOptionTextBrush" Color="#FFE8F0FA" />
      <Style x:Key="{x:Type TextBlock}" TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource SettingsOptionTextBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
      </Style>
      <Style x:Key="{x:Type Button}" TargetType="{x:Type Button}">
        <Setter Property="Foreground" Value="#FFFFFFFF" />
        <Setter Property="Background" Value="#FF111B2A" />
        <Setter Property="BorderBrush" Value="#FF24344A" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Padding" Value="16,9" />
        <Setter Property="Margin" Value="6" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type Button}">
              <Border Name="Root" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" CornerRadius="6" Margin="0">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter Property="Background" Value="#FF17263A" />
                  <Setter Property="BorderBrush" Value="#FF2E8BFF" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                  <Setter Property="Background" Value="#FF0A1320" />
                  <Setter Property="BorderBrush" Value="#FF8FC7FF" />
                  <Setter Property="UIElement.Opacity" Value="0.86" />
                  <Setter TargetName="Root" Property="Margin" Value="1,1,0,0" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type CheckBox}" TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{StaticResource SettingsOptionTextBrush}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin" Value="0,5" />
        <Setter Property="FrameworkElement.Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type CheckBox}">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="22" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Name="Box" Width="16" Height="16" CornerRadius="4" Background="#FF111B2A" BorderBrush="#FF31455F" BorderThickness="1" VerticalAlignment="Center" SnapsToDevicePixels="True">
                  <Path Name="CheckMark" Data="M3,8 L7,12 L13,4" Stroke="#FFF2F7FF" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Visibility="Collapsed" />
                </Border>
                <ContentPresenter Grid.Column="1" VerticalAlignment="Center" RecognizesAccessKey="True" />
              </Grid>
              <ControlTemplate.Triggers>
                <Trigger Property="ToggleButton.IsChecked" Value="True">
                  <Setter TargetName="Box" Property="Border.Background" Value="#FF2F8CFF" />
                  <Setter TargetName="Box" Property="Border.BorderBrush" Value="#FF2F8CFF" />
                  <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Box" Property="Border.BorderBrush" Value="#FF58A6FF" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="UIElement.Opacity" Value="0.55" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="SwitchCheckBoxStyle" TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{StaticResource SettingsOptionTextBrush}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin" Value="0,6" />
        <Setter Property="FrameworkElement.Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type CheckBox}">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="46" />
                </Grid.ColumnDefinitions>
                <ContentPresenter VerticalAlignment="Center" RecognizesAccessKey="True" />
                <Border Name="SwitchTrack" Grid.Column="1" Width="42" Height="22" CornerRadius="11" Background="#FF1B2633" BorderBrush="#FF253244" BorderThickness="1" HorizontalAlignment="Right" SnapsToDevicePixels="True" UseLayoutRounding="True" ClipToBounds="True">
                  <Border Name="SwitchThumb" Width="18" Height="18" Margin="2,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" CornerRadius="9" Background="#FFB8C4D6" SnapsToDevicePixels="True" UseLayoutRounding="True" />
                </Border>
              </Grid>
              <ControlTemplate.Triggers>
                <Trigger Property="ToggleButton.IsChecked" Value="True">
                  <Setter TargetName="SwitchTrack" Property="Border.Background" Value="#FF2F8CFF" />
                  <Setter TargetName="SwitchTrack" Property="Border.BorderBrush" Value="#FF2F8CFF" />
                  <Setter TargetName="SwitchThumb" Property="Border.Background" Value="#FFF2F7FF" />
                  <Setter TargetName="SwitchThumb" Property="Margin" Value="22,0,0,0" />
                  <TriggerBase.EnterActions>
                    <BeginStoryboard>
                      <Storyboard>
                        <ThicknessAnimation Storyboard.TargetName="SwitchThumb" Storyboard.TargetProperty="Margin" To="22,0,0,0" Duration="0:0:0.14">
                          <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut" />
                          </ThicknessAnimation.EasingFunction>
                        </ThicknessAnimation>
                      </Storyboard>
                    </BeginStoryboard>
                  </TriggerBase.EnterActions>
                  <TriggerBase.ExitActions>
                    <BeginStoryboard>
                      <Storyboard>
                        <ThicknessAnimation Storyboard.TargetName="SwitchThumb" Storyboard.TargetProperty="Margin" To="2,0,0,0" Duration="0:0:0.14">
                          <ThicknessAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut" />
                          </ThicknessAnimation.EasingFunction>
                        </ThicknessAnimation>
                      </Storyboard>
                    </BeginStoryboard>
                  </TriggerBase.ExitActions>
                </Trigger>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="SwitchTrack" Property="Border.BorderBrush" Value="#FF3A95FF" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="UIElement.Opacity" Value="0.55" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type ComboBox}" TargetType="{x:Type ComboBox}">
        <Setter Property="Margin" Value="0,5" />
        <Setter Property="MinWidth" Value="160" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource SettingsOptionTextBrush}" />
        <Setter Property="Background" Value="#FF111B2A" />
        <Setter Property="BorderBrush" Value="#FF31455F" />
        <Setter Property="Padding" Value="12,7" />
        <Setter Property="UIElement.SnapsToDevicePixels" Value="True" />
        <Setter Property="ItemContainerStyle">
          <Setter.Value>
            <Style TargetType="{x:Type ComboBoxItem}">
              <Setter Property="Foreground" Value="{StaticResource SettingsOptionTextBrush}" />
              <Setter Property="Background" Value="#FF111B2A" />
              <Setter Property="Padding" Value="12,8" />
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border Name="ItemRoot" Background="{TemplateBinding Control.Background}" Padding="{TemplateBinding Control.Padding}">
                      <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="ComboBoxItem.IsHighlighted" Value="True">
                        <Setter TargetName="ItemRoot" Property="Border.Background" Value="#FF17263A" />
                      </Trigger>
                      <Trigger Property="ListBoxItem.IsSelected" Value="True">
                        <Setter TargetName="ItemRoot" Property="Border.Background" Value="#FF183A66" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </Setter.Value>
        </Setter>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ComboBox}">
              <Grid>
                <ToggleButton Name="ToggleButton" ClickMode="Press" Background="{TemplateBinding Control.Background}" Foreground="{TemplateBinding Control.Foreground}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="1" Padding="{TemplateBinding Control.Padding}" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                  <Control.Template>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                      <Border Name="Root" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" CornerRadius="7" SnapsToDevicePixels="True">
                        <Grid>
                          <TextBlock Name="PART_SelectedText" Margin="{TemplateBinding Control.Padding}" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ComboBox}}" Text="{Binding Tag, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                          <Path Data="M0,0 L4,4 L8,0" Stroke="#FFB8C4D4" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0" />
                        </Grid>
                      </Border>
                      <ControlTemplate.Triggers>
                        <Trigger Property="UIElement.IsMouseOver" Value="True">
                          <Setter TargetName="Root" Property="Border.BorderBrush" Value="#FF58A6FF" />
                        </Trigger>
                      </ControlTemplate.Triggers>
                    </ControlTemplate>
                  </Control.Template>
                </ToggleButton>
                <Popup Name="PART_Popup" IsOpen="{TemplateBinding ComboBox.IsDropDownOpen}" AllowsTransparency="True" Placement="Bottom" PopupAnimation="Fade">
                  <Border Background="#FF0D1724" BorderBrush="#FF31455F" BorderThickness="1" CornerRadius="7" Margin="0,4,0,0" MinWidth="{TemplateBinding FrameworkElement.ActualWidth}">
                    <ScrollViewer MaxHeight="260">
                      <ItemsPresenter />
                    </ScrollViewer>
                  </Border>
                </Popup>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type TextBox}" TargetType="{x:Type TextBox}">
        <Setter Property="Margin" Value="0,5" />
        <Setter Property="MinWidth" Value="60" />
        <Setter Property="MinHeight" Value="28" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource SettingsOptionTextBrush}" />
        <Setter Property="TextBoxBase.CaretBrush" Value="#FF58A6FF" />
        <Setter Property="Background" Value="#FF111B2A" />
        <Setter Property="BorderBrush" Value="#FF31455F" />
        <Setter Property="Padding" Value="7,4" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type TextBox}">
              <Border Name="Root" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" CornerRadius="7" SnapsToDevicePixels="True">
                <ScrollViewer Name="PART_ContentHost" Margin="{TemplateBinding Control.Padding}" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsKeyboardFocusWithin" Value="True">
                  <Setter TargetName="Root" Property="Border.BorderBrush" Value="#FF58A6FF" />
                </Trigger>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Root" Property="Border.BorderBrush" Value="#FF3A5678" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
        <Setter Property="Width" Value="10" />
        <Setter Property="Background" Value="#00FFFFFF" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ScrollBar}">
              <Grid Background="#00FFFFFF">
                <Track Name="PART_Track" IsDirectionReversed="True">
                  <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" />
                  </Track.DecreaseRepeatButton>
                  <Track.Thumb>
                    <Thumb Background="#FF26384F">
                      <Control.Template>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                          <Border Name="ThumbRoot" Background="{TemplateBinding Control.Background}" CornerRadius="5" Margin="2" />
                          <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                              <Setter TargetName="ThumbRoot" Property="Border.Background" Value="#FF3A5678" />
                            </Trigger>
                            <Trigger Property="Thumb.IsDragging" Value="True">
                              <Setter TargetName="ThumbRoot" Property="Border.Background" Value="#FF2F8CFF" />
                            </Trigger>
                          </ControlTemplate.Triggers>
                        </ControlTemplate>
                      </Control.Template>
                    </Thumb>
                  </Track.Thumb>
                  <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" />
                  </Track.IncreaseRepeatButton>
                </Track>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
        <Style.Triggers>
          <Trigger Property="ScrollBar.Orientation" Value="Horizontal">
            <Setter Property="Width" Value="Auto" />
            <Setter Property="Height" Value="10" />
          </Trigger>
        </Style.Triggers>
      </Style>
      <Style x:Key="{x:Type Slider}" TargetType="{x:Type Slider}">
        <Setter Property="Margin" Value="0,6" />
        <Setter Property="MinWidth" Value="180" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type Slider}">
              <Grid>
                <Border Height="6" CornerRadius="3" Background="#FF1B2A3C" BorderBrush="#FF2A3E58" BorderThickness="1" VerticalAlignment="Center" />
                <Track Name="PART_Track" VerticalAlignment="Center">
                  <Track.DecreaseRepeatButton>
                    <RepeatButton Command="Slider.DecreaseLarge" Background="#FF1677FF" BorderThickness="0" Height="6" Opacity="0.95" />
                  </Track.DecreaseRepeatButton>
                  <Track.IncreaseRepeatButton>
                    <RepeatButton Command="Slider.IncreaseLarge" Background="#00FFFFFF" BorderThickness="0" Height="6" />
                  </Track.IncreaseRepeatButton>
                  <Track.Thumb>
                    <Thumb Width="18" Height="18" Background="#FFEAF2FF" BorderBrush="#FF58A6FF" BorderThickness="2">
                      <Control.Template>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                          <Ellipse Fill="{TemplateBinding Control.Background}" Stroke="{TemplateBinding Control.BorderBrush}" StrokeThickness="{TemplateBinding Control.BorderThickness}" />
                        </ControlTemplate>
                      </Control.Template>
                    </Thumb>
                  </Track.Thumb>
                </Track>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type ListBox}" TargetType="{x:Type ListBox}">
        <Setter Property="Background" Value="#99101B28" />
        <Setter Property="BorderBrush" Value="#FF223247" />
        <Setter Property="Foreground" Value="{StaticResource SettingsOptionTextBrush}" />
        <Setter Property="FontSize" Value="15" />
      </Style>
      <Style x:Key="{x:Type ListBoxItem}" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Foreground" Value="{StaticResource SettingsOptionTextBrush}" />
        <Setter Property="Padding" Value="10,9" />
        <Setter Property="Margin" Value="0,2" />
      </Style>
      <Style x:Key="TitleBarButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Width" Value="46" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="#00FFFFFF" />
        <Setter Property="Foreground" Value="#FFE8F0FA" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type Button}">
              <Border Name="Root" Background="{TemplateBinding Control.Background}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Root" Property="Border.Background" Value="#FF13263D" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                  <Setter TargetName="Root" Property="Border.Background" Value="#FF0B1A2B" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="CloseTitleBarButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource TitleBarButtonStyle}">
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type Button}">
              <Border Name="Root" Background="{TemplateBinding Control.Background}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Root" Property="Border.Background" Value="#FFC42B2B" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                  <Setter TargetName="Root" Property="Border.Background" Value="#FF8F1D1D" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid Name="SettingsRoot">
    <Grid.RowDefinitions>
      <RowDefinition Height="36" />
      <RowDefinition Height="*" />
      <RowDefinition Height="68" />
    </Grid.RowDefinitions>
    <Grid Name="HeaderBar" Row="0" Background="#EE050B12" MouseLeftButtonDown="HeaderBar_MouseLeftButtonDown">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
        <Image Source="/app.ico" Width="20" Height="20" Stretch="Uniform" Margin="0,0,10,0" />
        <TextBlock VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold">
          <Run Text="Ремский" Foreground="#FFF2F2F2" /> <Run Text=" Мониторинг" Foreground="#FFFF6A00" /></TextBlock>
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="─" Style="{StaticResource TitleBarButtonStyle}" Click="MinimizeButton_Click" />
        <Button Name="MaximizeButton" Content="□" Style="{StaticResource TitleBarButtonStyle}" Click="MaximizeButton_Click" />
        <Button Content="×" Style="{StaticResource CloseTitleBarButtonStyle}" FontSize="18" Click="CloseButton_Click" />
      </StackPanel>
    </Grid>
    <Grid Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="235" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="390" />
      </Grid.ColumnDefinitions>
      <Border Name="SidebarPanel" Grid.Column="0" Background="#DD0A1521" BorderBrush="#1FFFFFFF" BorderThickness="0,0,1,0" Padding="14">
        <DockPanel>
          <StackPanel DockPanel.Dock="Bottom">
            <Button Content="▣  Сохранить настройки" HorizontalAlignment="Stretch" MinWidth="196" Click="SaveSettingsFileButton_Click" />
            <Button Content="⇩  Загрузить настройки" HorizontalAlignment="Stretch" MinWidth="196" Click="LoadSettingsFileButton_Click" />
            <Button Content="↻  Сбросить настройки" HorizontalAlignment="Stretch" MinWidth="196" Click="ResetSettingsButton_Click" />
          </StackPanel>
          <ListBox Name="SectionList" BorderThickness="0" SelectionChanged="SectionList_SelectionChanged">
            <ListBoxItem Content="Общие" />
            <ListBoxItem Content="FPS" />
            <ListBoxItem Content="Видеокарта" />
            <ListBoxItem Content="Процессор" />
            <ListBoxItem Content="ОЗУ" />
            <ListBoxItem Content="Статистика" />
            <ListBoxItem Content="График времени кадра" />
            <ListBoxItem Content="Внешний вид" />
            <ListBoxItem Content="Горячие клавиши" />
            <ListBoxItem Content="Активация" />
            <ListBoxItem Content="Обновления" />
            <ListBoxItem Content="Оставить отзыв" />
          </ListBox>
        </DockPanel>
      </Border>
      <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
        <StackPanel Name="ContentPanel" Margin="22" />
      </ScrollViewer>
      <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="14,22,22,22">
          <TextBlock Text="Предпросмотр оверлея" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,14" />
          <Border Name="PreviewRoot" Background="#D0080808" BorderBrush="#44576A80" BorderThickness="1" CornerRadius="10" Padding="12">
            <StackPanel Name="PreviewStack">
              <Grid Name="PreviewBrandPanel" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                  <TextBlock Name="PreviewBrandText" FontFamily="Segoe UI" FontWeight="SemiBold" FontSize="21" TextAlignment="Left" LineHeight="22">
                    <Run Text="Ремский" Foreground="#FFF2F2F2" />
                    <LineBreak />
                    <Run Text="Мониторинг" Foreground="#FFFF6A00" />
                  </TextBlock>
                  <TextBlock Name="PreviewBrandUrlText" Text="r-mont.ru" Foreground="#FFFF6A00" FontFamily="Segoe UI" FontWeight="SemiBold" FontSize="14" Margin="0,2,0,0" />
                </StackPanel>
                <Image Name="PreviewBrandIcon" Grid.Column="1" Source="/brandpic.png" Height="64" Margin="12,0,0,0" HorizontalAlignment="Right" VerticalAlignment="Center" />
              </Grid>
              <StackPanel Name="PreviewFpsBlock">
                <TextBlock Name="PreviewTime" Text="00:00:00" FontFamily="Segoe UI" FontWeight="Bold" FontSize="16" TextAlignment="Left" HorizontalAlignment="Left" Margin="0,0,0,4" />
                <Grid Name="PreviewFpsHeader" Margin="0,0,0,8">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,2">
                    <TextBlock Name="PreviewFpsLabel" Text="FPS" FontWeight="Bold" FontSize="24" />
                  </StackPanel>
                  <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Name="PreviewFpsValue" Text="143" Foreground="#FFFF6A00" FontWeight="Bold" FontSize="24" TextAlignment="Right" />
                  </StackPanel>
                </Grid>
                <TextBlock Name="PreviewFpsDetails" Text="AVG: 142   1% Low: 98   0.1% Low: 75" FontFamily="Segoe UI" FontSize="12" />
                <TextBlock Name="PreviewGame" Text="Окно: N/A" FontFamily="Segoe UI" FontSize="11" Margin="0,2,0,8" />
                <TextBlock Name="PreviewApi" Text="API: N/A  N/A" FontFamily="Segoe UI" FontWeight="Bold" FontSize="12" />
                <Rectangle Name="PreviewSeparator" Height="1" Fill="#33FFFFFF" Margin="0,8,0,9" />
              </StackPanel>
              <StackPanel Name="PreviewGpu" />
              <StackPanel Name="PreviewCpu" />
              <StackPanel Name="PreviewRam" />
              <StackPanel Name="PreviewStats" />
              <Canvas Name="PreviewGraph" Height="52" Background="#FF000000" Margin="0,10,0,0" ClipToBounds="True" />
              <TextBlock Name="PreviewGraphLabel" Text="7.1 ms" FontFamily="Segoe UI" FontWeight="Bold" FontSize="11" Foreground="#FFFF0000" />
            </StackPanel>
          </Border>
          <Border Name="HintPanel" Background="#66101B28" CornerRadius="8" Padding="16" Margin="0,16,0,0">
            <TextBlock TextWrapping="Wrap" Foreground="#FFB8C4D4" Text="Превью обновляется сразу. Реальный оверлей изменится только после кнопки «Применить»." />
          </Border>
        </StackPanel>
      </ScrollViewer>
    </Grid>
    <Border Name="FooterBar" Grid.Row="2" Background="#EE050B12" BorderBrush="#1FFFFFFF" BorderThickness="0,1,0,0">
      <Grid Margin="22,10,22,10">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Name="FreeVersionText" Text="Бесплатная версия программы" Foreground="#99FFFFFF" FontFamily="Segoe UI" FontSize="22" VerticalAlignment="Center" Visibility="Collapsed" />
        <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
          <Button Name="ApplyButton" Content="Применить" Background="#FF1677FF" BorderBrush="#FF2E8BFF" MinWidth="130" Click="ApplyButton_Click" />
          <Button Name="CancelButton" Content="Отмена" MinWidth="130" Click="CancelButton_Click" />
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</Window>